# 文書配置ガイド

## 読者別構成

| フォルダー | 読者 | 配置する文書 |
| --- | --- | --- |
| `users/` | テンプレートから作成したリポジトリの利用者 | プロジェクト運営、開発、運用に使う文書 |
| `contributors/` | このテンプレートリポジトリへの貢献者 | テンプレート自体の設計、保守、改善に使う文書 |

- `docs/` 直下には、この配置ガイドだけを置く。
- 両方が参照する文書は、主な読者側に置き、もう一方からリンクする。
- 各読者フォルダー内は、次の文書種別で分類する。
- 一時的な調査結果は関連する恒久文書へ統合する。統合できない場合は Issue に残す。

## 文書種別

| フォルダー | 責務 | 配置する文書 |
| --- | --- | --- |
| `project/` | プロジェクト固有の目的と体制 | Problem Statement、Success Criteria、Definition of Done、ステークホルダー |
| `processes/` | 繰り返し実行する業務プロセス | Scrum の責務、Backlog 管理、イベントの進め方 |
| `policies/` | 守るべき規則と標準 | ブランチ保護、ラベル体系、Issue Template の使い分け、レビュー基準 |
| `guides/` | 特定作業の手順と学習資料 | セットアップ、機能選定、学習ガイド |
| `architecture/` | システム構造と技術判断 | アーキテクチャ概要、ADR、データフロー、外部連携 |
| `nfr/` | 検証可能な非機能要件 | セキュリティ、性能、信頼性、コスト、運用性の要件 |
| `operations/` | 稼働後の運用手順 | デプロイ、監視、障害対応、バックアップ、復旧手順 |

- 要件は `nfr/`、要件を満たす運用手順は `operations/` に分ける。

## 既存文書の棚卸し

| 配置 | 文書 | 分類結果 |
| --- | --- | --- |
| `users/processes/` | `backlog-management.md`、`scrum-accountabilities.md`、`scrum-ceremonies-agenda.md` | ユーザー リポジトリで繰り返し使う Scrum プロセス |
| `users/policies/` | `branch-policy.md`、`issue-templates.md`、`labels.md` | ユーザー リポジトリで適用する規則と標準 |
| `users/guides/` | `setup.md`、`azure-learning-guide.md` | ユーザー向けの作業手順と学習資料 |
| `contributors/guides/` | `github-security-features.md` | テンプレート自体の保守に使う機能選定資料 |

- 重複: `setup.md` は規則の詳細を持たず、各ポリシーを参照するため重複なし。
- 分類上の課題: 解消済み。読者と文書種別の両方で配置を判断できる。
- 移動候補: なし。新しい責務が生じた場合は、既存文書への統合可否を先に確認する。
- `users/project/` と `users/nfr/` はセットアップ Issue により文書が作成されるまで空とする。

## 追加・移動の判断基準

1. 文書の主目的を標準構成の責務に照らして、配置先を 1 つ選ぶ。
2. 主な読者がテンプレート利用者か、このリポジトリへの貢献者かを決める。
3. 同じ目的の文書を検索し、追記または統合で済まない場合だけ新規作成する。
4. 対象読者が同じでも、主目的が異なる文書は分ける。
5. 移動時はリポジトリ内の相対リンクと自動生成 Issue の参照先を更新する。
6. 新しい分類が必要な場合は、文書を追加する前にこのガイドへ責務と対象を追記する。

## 命名規則

- ファイル名とフォルダー名は小文字の kebab-case にする。
- ファイル名は内容を表す名詞句にし、`document.md` や `misc.md` を避ける。
- 日付や版番号は履歴管理に Git を使い、原則としてファイル名に含めない。
- 同じ主題を複数ファイルに分割する場合は、主題名のサブフォルダーを作り、その中に `README.md` を置く。

## レビュー観点

- 標準フォルダーの責務と文書の主目的が一致しているか。
- `users/` と `contributors/` のどちらが主な読者か明確か。
- 既存文書への追記や統合ではなく、新規文書が必要か。
- 要件、設計、運用手順が 1 文書に混在していないか。
- 相対リンク、README、Issue テンプレート、Copilot 指示の参照先が有効か。
- 将来も使う決定だけが残り、一時的な議論や作業メモが混入していないか。

## 今回の対象外

- `users/architecture/` と `users/operations/` の文書作成。成果物が必要になった時点で追加する。
- セットアップ Issue によりユーザー リポジトリで作成される `users/project/` と `users/nfr/` の初期文書。
- `contributors/guides/github-security-features.md` と `users/guides/azure-learning-guide.md` の内容更新。
- 実プロジェクト経験者とのレビューで得られる追加知見。レビュー後に別 Issue で反映する。
